---
# Removes the url from bosh-libvirt-cpi release so bosh create-env uses
# the current working directory (repo root) to build a dev release.
- type: remove
  path: /releases/name=bosh-libvirt-cpi/url
