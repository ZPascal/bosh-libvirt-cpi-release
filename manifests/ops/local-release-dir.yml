---
# Sets bosh-libvirt-cpi release URL to an absolute local path so
# bosh create-env builds a dev release from the repo root directory.
- type: replace
  path: /releases/name=bosh-libvirt-cpi/url
  value: file://((release_dir))
