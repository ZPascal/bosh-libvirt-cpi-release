---
# Sets a fixed MAC address on the Director VM so the libvirt DHCP
# reservation for 192.168.122.10 takes effect.
- type: replace
  path: /resource_pools/name=vms/cloud_properties/mac?
  value: "52:54:00:ab:cd:10"
